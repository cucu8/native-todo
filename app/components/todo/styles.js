import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    todoContainer: {
        padding: 20,
        marginTop: 20,
        borderWidth: 1,
        borderRadius: 10,
        backgroundColor: "#3B3B3B",
        borderColor: "#525252",
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
    },

    todoText: {
        color: "#FFFFFF",
        fontSize: 16,
        flex: 1,
    }
})